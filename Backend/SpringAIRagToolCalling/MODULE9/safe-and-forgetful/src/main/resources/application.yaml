spring:
  application:
    name: safe-and-forgetful

  ai:
    openai:
      api-key: ${OPENAI_API_KEY}
      chat:
        options:
          model: gpt-4o-mini
          temperature: 0.7

logging:
  level:
    org.springframework.ai: DEBUG

